# Minimum Spanning Tree (MST) Problem

The minimum spanning tree problem consists of finding a subset of edges in a graph,
that connect all nodes without creating cycles, such that the sum of the costs of these edges is minimized.
This model as a MIP requires subtour elimination constraints.
For this purpose, all subsets of nodes (generated by a power set) are used.
